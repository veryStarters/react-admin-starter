// reducers初始状态
const initState = {}

// 在此处定义reducers
export const reducers = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

// 此处定义actions
export const actions = {
  // normal demo
  // test1() {
  //   return dispatch => {
  //   }
  // },
  // async demo, return a promise
  // test2() {
  //   return (dispatch, getState) => {
  //     return Promise.resolve(getState())
  //   }
  // }
}
